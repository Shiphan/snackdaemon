daemon
	check if daemon not running
		fork
		if main
			
		else child
			open daemon in shared memory
			wait for kill
			kill
			end
	else
		error
	end

kill
	check if daemon running
		send kill to daemon
	end

update
	check if daemon running
		use daemon to update snackbar
	else
		error
	end

close
	check if daemon running
		use daemon to close snackbar
	else
		error
	end
